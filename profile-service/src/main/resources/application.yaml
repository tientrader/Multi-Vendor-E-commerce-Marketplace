server:
  port: 8081
  servlet:
    context-path: /profile

spring:
  application:
    name: profile-service
  neo4j:
    uri: ${DBMS.CONNECTION}
    authentication:
      username: ${DBMS_USERNAME}
      password: ${DBMS_PASSWORD}